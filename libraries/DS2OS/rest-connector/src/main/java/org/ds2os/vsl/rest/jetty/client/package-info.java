/**
 * Jetty client components which are also used for the transport module (server) which also needs
 * client connections to other KAs. This part of the code does not require HTTP server components.
 *
 * @author felix
 */
package org.ds2os.vsl.rest.jetty.client;
